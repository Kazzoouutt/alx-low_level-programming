#include<stdio.h>
/* The Alphabet Game */

/**
 * main - A program that prints the alphabets in lowercase followed by a new line
 * Return: Always 0 (Success)
 */
int main (void)
{
	for (int x = a; x <= z; x++)
	{
		putchar (x);
		putchar (\n);
	}
	return (0);
}		
