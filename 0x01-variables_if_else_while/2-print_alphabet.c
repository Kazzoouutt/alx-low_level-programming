#include<stdio.h>
/* The Alphabet Game */

/**
 * main - A program that prints the alphabets in lowercase followed by a new line
 * Return: Always 0 (Success)
 */
int main (void)
{
	for (int i = 97; ch <= 123; i++)
	{
		putchar(i);
	}
	putchar(10);
	return (0);
}		
